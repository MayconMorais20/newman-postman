{
  "name": "placeholder-postman-jenkins",
  "version": "1.0.0",
  "description": "Project for run tests with jenkins",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "api-tests-production": "newman run tests/FAKE-API.postman_collection.json --reporters junit --reporter-junit-export newman.xml"
  },
  "author": "Maycon Douglas",
  "dependencies": {
    "newman": "^5.3.1"
  }
}
